# Copy this file to .env for local runs (never commit .env)
# On Render: add MONGODB_URI in Dashboard → Environment

MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority
PORT=3000

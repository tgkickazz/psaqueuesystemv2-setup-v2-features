# Logs (optional — log/ folder is tracked for full project backup)
*.log
npm-debug.log*

# Secrets and local config (never commit)
.env
.env.*
!.env.example
credentials.json

# Archives / temp
public.7z
*.7z
temp_log_inspect.js
*.tmp
*.bak
*~

# OS
.DS_Store
Thumbs.db

# Optional: local user dumps (uncomment if these hold real accounts)
# users.json
# roles.json

# One-time helper (optional; do not commit)
cleanup-repo-and-push.ps1
